<%= nested_form_for @recipe do |f| %>
  <%= f.error_messages %>
  <p> 
  <%= f.label :title %><br />
  <%= f.text_field :title %>
  </p>
    <%= f.fields_for :steps %>
    <p><%= f.link_to_add "Add Step", :steps %></p>
    <p><%= f.submit "Submit" %></p>
<% end %>
